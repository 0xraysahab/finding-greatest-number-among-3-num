#include <stdio.h>

int main() {
    int a,b,c,big;
    printf("enter three number:\n ");
    scanf("%d%d%d", &a, &b, &c);
    big = (a>b && b>c)? (a) : (b>c)? b:c;
    printf("%d",big);
    return 0;
}
